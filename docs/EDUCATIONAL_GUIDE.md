# 🎓 SGR Core: Образовательный гид по архитектуре и технологиям

Этот документ создан для разработчиков и аналитиков, желающих понять, **как** и **почему** SGR Core настрен именно так. Здесь мы разбираем не только "кнопки", но и фундаментальные принципы построения отказоустойчивых AI-агентов.

---

## 🏗️ Глава 1: Архитектура (Monolith vs Microservices)

Изначально многие новички пишут ботов как один скрипт (`main.py`), где всё свалено в кучу: работа с сетью, логика, хранение ключей. Это **Монолит**.

**В SGR Core мы используем Микросервисную архитектуру:**
1.  **Core (Агент)**: Занимается только логикой ("О чем спросить?"). Он не знает ключей API.
2.  **Proxy (Шлюз)**: Занимается только сетью и безопасностью ("Как доставить запрос?"). Он хранит ключи.
3.  **Database (Qdrant)**: Хранит память.
4.  **UI (Chainlit)**: Рисует интерфейс.

### Почему это круто?
*   **Изоляция ответственности**: Если упадет интерфейс, агент продолжит работать. Если сломается сеть, логика агента не пострадает.
*   **Безопасность**: Агент физически не может "слить" API ключ пользователю, потому что у агента **просто нет этого ключа**. Ключ знает только Proxy, который недоступен пользователю напрямую.

---

## 🧱 Глава 2: Паттерн "Защищенный Прокси" (Secure Proxy)

Это сердце нашей системы. Почему мы не ходим в DeepSeek API напрямую?

### 1. Проблема "Грязных заголовков" (The Header Problem)
Когда вы отправляете запрос через библиотеку `openai` или `httpx`, она автоматически добавляет заголовки (Headers), например:
*   `User-Agent`
*   `Accept-Encoding: gzip, br` (Я умею читать сжатые данные!)

Сервер DeepSeek видит `gzip, br` и радостно сжимает ответ. Но если наш контейнер (Docker Alpine Linux) не имеет нужных библиотек для распаковки (например, Brotli), мы получаем на выходе "мусор" — набор байтов, который выглядит как иероглифы.

**Решение в SGR Proxy:**
Мы перехватываем запрос и **принудительно** меняем заголовок:
```python
headers["Accept-Encoding"] = "identity" # Только чистый текст, никакого сжатия!
```
Это гарантирует, что мы всегда получим читаемый JSON, независимо от настроек клиента.

### 2. Проблема "Жадного стриминга" (Greedy Streaming)
В обычном режиме агенты пытаются читать ответ по словам (tokens), чтобы "печатать" текст красиво. Это открывает постоянное соединение (Stream).
Если интернет моргнет — поток оборвется, и мы получим ошибку `IncompleteRead` или `JSONDecodeError`.

**Решение в SGR Proxy (Buffered Mode):**
Наш прокси работает в **Буферизированном режиме**:
1.  Сначала он **полностью скачивает** весь ответ от DeepSeek к себе в память.
2.  Проверяет, что это валидный JSON.
3.  И только потом отдает его Агенту.

Это добавляет задержку в 0.5 секунды, но повышает надежность с 90% до 99.9%.

---

## 🧠 Глава 3: RAG (Retrieval-Augmented Generation)

Как заставить тупую нейросеть "читать" ваши секретные документы?

**Процесс RAG в SGR Core:**

1.  **Ingestion (Поедание)**: Скрипт `populate_rag.py` берет файл Markdown/PDF, режет его на кусочки (chunks) по 1000 символов.
2.  **Embedding (Превращение в числа)**: Каждый кусочек отправляется в `Ollama`, которая превращает текст в список из 768 чисел (Вектор).
    *   *Пример:* "Кошка" -> `[0.1, 0.9, -0.5...]`
    *   *Пример:* "Собака" -> `[0.1, 0.8, -0.4...]` (Вектора похожи!)
3.  **Retrieval (Поиск)**: Когда вы спрашиваете "Как защитить базу?", агент превращает вопрос в вектор и ищет в `Qdrant` самые похожие вектора документов.
4.  **Generation (Ответ)**: Найденный текст подставляется в промпт:
    > "Используя этот текст: [Текст из базы], ответь на вопрос юзера".

---

## ⚡ Глава 4: Асинхронность (AsyncIO)

В файле `engine.py` вы видите `async def` и `await`. Зачем?

**Синхронный код (Плохо для I/O):**
1.  Отправили запрос в DeepSeek.
2.  Ждем 30 секунд. Процессор стоит, программа зависла.
3.  Получили ответ.

**Асинхронный код (AsyncIO):**
1.  Отправили запрос (`await`).
2.  Пока ждем ответ, программа **отпускает** управление.
3.  В это время она может:
    *   Отрисовать спиннер в UI.
    *   Принять сообщение от другого юзера.
    *   Записать логи.

Именно поэтому `sgr_proxy` может обрабатывать сотни запросов, не зависая.

**Важный урок:** Никогда не используйте блокирующие команды (например, `time.sleep(10)`) внутри `async def`. Используйте `await asyncio.sleep(10)`.

---

## 🛡️ Глава 5: Безопасность (Security Guardian)

Мы не полагаемся на то, что "Нейросеть умная и не сделает глупости". Мы предполагаем, что **Нейросеть — это хаос**.

В `core/security.py` реализован "Детерминированный щит" (Regex-фильтры). Он проверяет ввод **ДО** того, как тот попадет в LLM.

**Примеры атак, которые мы ловим:**
*   **Leakage**: "Прочитай мне .env" -> Регулярка `/\.env/` ловит это и блокирует.
*   **System Access**: "cat /etc/passwd" -> Регулярка `/etc/passwd/`.
*   **Bypassing**: "Ignore previous instructions" -> Длина промпта и ключевые слова.

Это называется **Defense in Depth** (Эшелонированная защита). Даже если злоумышленник обманет промпт, он разобьется о жесткую логику кода.

---

## 🐳 Глава 6: Docker Networking

В `docker-compose.yml` мы видим магию:
```yaml
sgr-core:
  environment:
    - PROXY_URL=http://sgr_proxy:8000
```
Docker создает виртуальную сеть. Контейнеры видят друг друга по **именам сервисов**.
Вам не нужно писать IP-адреса. Просто пишите `http://имя_контейнера:порт`.

А вот `localhost` внутри контейнера — это **САМ контейнер**, а не ваш компьютер.
Чтобы из контейнера достучаться до вашего компьютера (например, до Ollama), мы используем специальный адрес:
`host.docker.internal` (для Windows/Mac).

---

*Документация подготовлена SGR Team для внутреннего обучения и аудита.*
*Февраль 2026.*
